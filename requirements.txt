# ===========================================
# 学科知识整合智能体 - Python 依赖
# 安装: pip install -r requirements.txt
# ===========================================

# ---------- Web 框架 ----------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19    # 文件上传（multipart/form-data）

# ---------- 文件解析 ----------
pdfplumber==0.11.9          # PDF 解析
python-docx==1.1.2          # Word .docx 解析
openpyxl==3.1.5             # Excel .xlsx 解析
# Markdown 由 src/parsers/markdown_parser.py 自行按标题切分，无需额外依赖

# ---------- LLM ----------
# DeepSeek / 通义千问（DashScope 兼容模式）/ OpenAI 均通过 OpenAI 兼容接口调用
openai==1.68.0

# ---------- 嵌入 ----------
# sentence-transformers 不可用（离线/未下载模型）时自动降级为 hashing 后端，
# 因此它是可选依赖：只想要 hashing 后端可以不装，装了就自动获得更好的语义检索。
sentence-transformers==3.4.1
torch>=2.2                  # PyTorch（sentence-transformers 的运行依赖，CPU 版即可）

# ---------- 向量检索 ----------
faiss-cpu>=1.8              # 未安装时自动降级为 numpy 精确检索

# ---------- 工具库 ----------
python-dotenv==1.0.1        # 环境变量管理
pydantic==2.10.5            # 数据验证
pydantic-settings==2.7.0    # 配置管理
httpx==0.28.1               # HTTP 客户端
numpy==2.2.2                # 数值计算
scikit-learn==1.6.1         # HashingVectorizer（hashing 降级后端）

# ---------- 测试 ----------
pytest>=8.0.0               # 测试框架
pytest-asyncio>=0.23.0      # 异步测试支持
